<div class="header_container">
    <div class="header_logo">
        <a href="/">
            <img src="/images/Afseem_Logo.png" alt="sitelogo">
            <span>Grocery & Grill, Perfectly Paired</span>
        </a>
    </div>
    <div class="header_menu">
        <div class="header_menu_item">
            <% if (user && user.role === 'Seller') { %>
                <a href="/store-dashboard">Dashboard</a>
                <a href="/store-profile">Profile</a>
                <a href="/add-product">Add Product</a>
                <a href="/product-upload">Upload Product</a>
                <a href="/store-logout">Logout</a>
            <% } else if(user && user.role === 'User') { %>
                <a href="/store-category">Store</a>
                <a href="/profile">Profile</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/registration">Register</a>
                <a href="/login">Login</a>
                <a href="/store-category">Store</a>
                <div class="cartIc"><i class="fa-solid fa-cart-shopping"></i></div>
            <% } %> %>
        </div>
        
        <div class="Icon_container">
            <% if (user && user.role === 'User') { %>
                <div class="cartIc"><i class="fa-solid fa-cart-shopping"></i></div>
            <% } %>
             <div class="menu_hamburder"><i class="fa-solid fa-bars"></i></div>
        </div>
    </div>
    <div class="mobile_menu">
            <% if (user && user.role === 'Seller') { %>
                <a href="/store-dashboard">Dashboard</a>
                <a href="/store-profile">Profile</a>
                <a href="/add-product">Add Product</a>
                <a href="/product-upload">Upload Product</a>
                <a href="/store-logout">Logout</a>
            <% } else if(user && user.role === 'User') { %>
                <a href="/store-category">Store</a>
                <a href="/profile">Profile</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/registration">Register</a>
                <a href="/login">Login</a>
                <a href="/store-category">Store</a>
                <div class="cartIc"><i class="fa-solid fa-cart-shopping"></i></div>
            <% } %> %>
        <div class="menuCross"><i class="fa-solid fa-xmark"></i></div>
    </div>
    
</div>

